# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 本文件只是「模板/说明书」，程序运行时不会靠它生效。
# 请在与 package.json 同级的文件「.env」里填写真实 App ID、Secret、表格 token。
# 若还没有 .env：复制本文件，改名为 .env（注意前面有点号），再用记事本编辑 .env。
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FEISHU_APP_ID=你的应用_App_ID
FEISHU_APP_SECRET=你的应用_App_Secret

# 用户 OAuth（顶部「飞书登录」）：与飞书开放平台「安全设置 → 重定向 URL」完全一致。
#
# 【推荐·本地】直连 sync-server，避免授权完成回跳依赖 Vite 端口（否则易出现 ERR_CONNECTION_REFUSED）：
# FEISHU_OAUTH_REDIRECT_URI=http://127.0.0.1:3789/api/auth/feishu/callback
# （须与 SERVER_PORT 一致；飞书后台须添加同一条重定向 URL。工作台页面仍可用 localhost:任意端口打开。）
#
# 【备选·本地】经 Vite 代理（授权完成时 Vite 必须在跑，且端口与 URL 一致）：
# FEISHU_OAUTH_REDIRECT_URI=http://127.0.0.1:5173/api/auth/feishu/callback
# scope 须为应用「权限管理」里已开通且已发布的用户身份权限的子集，多个用空格分隔。
# 仅填 offline_access 时，拉 docx 常会报 99991679：须包含云文档读权限之一，例如 docx:document:readonly（或 docx:document），改完后需重新点「飞书授权」换 token。
# FEISHU_OAUTH_SCOPE=offline_access docx:document:readonly

# 表格链接里的 token：https://xxx.feishu.cn/sheets/这一段（注意区分字母 I 与数字 1、小写 l，建议从浏览器地址栏复制）
FEISHU_SPREADSHEET_TOKEN=你的表格_token

# 子表名称（与飞书里工作表标签一致）
FEISHU_SHEET_TITLE=HESAI FA Tracking Sheet V1.0

# 本地飞书写入服务端口（云主机常用环境变量 PORT）。本地前端 + 同步服务可一条命令启动：
# npm.cmd run dev:all
SERVER_PORT=3789

# 同步写入时表头最大列数（默认 80）。飞书读 A1:ZZ1 会带回大量尾随空列，服务端会裁掉空列后再按此上限截断，避免 range 变成 A:ZZ 报错。
# FEISHU_MAX_SYNC_COLUMNS=80

# 按「单选/下拉」格式写入的列名（须与表格第 1 行表头完全一致，英文逗号分隔）。飞书列须已设置下拉，且写入值与某一选项文案完全一致（如 小米、中）。
# FEISHU_DROPDOWN_HEADERS=客户信息,优先级

# 追加写入模式：OVERWRITE（默认，优先写入已有空行，通常更能继承下拉验证）或 INSERT_ROWS（先插新行再写）。
# FEISHU_INSERT_DATA_OPTION=OVERWRITE

# --- 公网部署（可选）---
# 同步接口防滥用：设置后，POST /api/sync-feishu 与 /api/fetch-feishu-doc 须带请求头 X-Api-Key 或 Authorization: Bearer <值>
# 若前端构建时设置下面的 VITE_SYNC_API_KEY 为相同值，浏览器会自动带上（密钥仍会暴露在网页中，仅降低被扫接口的概率）
SYNC_API_KEY=

# 前端构建时写入（无尾斜杠），指向已部署的 sync-server 根地址，例如 https://your-app.railway.app
# 本地开发不要设置，沿用 Vite 代理到本机 3789
# VITE_FEISHU_API_BASE=
# VITE_SYNC_API_KEY=
