# FA Workbench

基于 Vite + React 的前端工作台；本地开发可配合 `server/sync-server.mjs` 使用。

## 开发

```bash
npm install
npm run dev
```

前后端一起启动：

```bash
npm run dev:all
```

## 环境变量

复制 `.env.example` 为 `.env` 并按说明填写。

## 构建

```bash
npm run build
```
